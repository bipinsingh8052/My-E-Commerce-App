import React from 'react'
import { SafeAreaView, ScrollView, Text, View } from 'react-native'

export default function explorepage() {
  return (
    <SafeAreaView>
      <ScrollView>
    <View>
      <Text>This is expolore page</Text>
    </View>
        
      </ScrollView>
    </SafeAreaView>
  )
}
